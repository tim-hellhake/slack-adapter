{
  "name": "slack-adapter",
  "display_name": "Slack",
  "version": "0.1.1",
  "description": "Send slack messages to your workspace",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tim-hellhake/slack-adapter.git"
  },
  "author": "Tim Hellhake",
  "license": "MPL-2.0",
  "bugs": {
    "url": "https://github.com/tim-hellhake/slack-adapter/issues"
  },
  "homepage": "https://github.com/tim-hellhake/slack-adapter#readme",
  "files": [
    "LICENSE",
    "SHA256SUMS",
    "index.js",
    "slack-adapter.js"
  ],
  "moziot": {
    "api": {
      "min": 2,
      "max": 2
    },
    "plugin": true,
    "exec": "{nodeLoader} {path}",
    "schema": {
      "type": "object",
      "required": [
        "webhookUrl"
      ],
      "properties": {
        "webhookUrl": {
          "type": "string",
          "title": "The slack webhook url"
        },
        "messages": {
          "type": "array",
          "title": "Predefined messages",
          "items": {
            "type": "object",
            "title": "A predefined message",
            "required": [
              "name",
              "message"
            ],
            "properties": {
              "name": {
                "type": "string",
                "title": "The name of the action"
              },
              "message": {
                "type": "string",
                "title": "The text of the message"
              }
            }
          }
        }
      }
    }
  },
  "dependencies": {
    "node-fetch": "^2.4.1"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "babel-eslint": "^10.0.1"
  }
}
